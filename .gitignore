node_modules/
dist/
.DS_Store
.env.local
.claude/

# Local capture output only (repo-root). Anchored so public/screenshots/ (served
# landing assets like the hero shot) stays tracked.
/screenshots/
design-system/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
