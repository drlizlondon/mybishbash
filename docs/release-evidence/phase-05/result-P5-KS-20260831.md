# Phase 5 — Packet A (staging kill-switch) result record

**Run ID:** `P5-KS-20260831`
**Date:** 2026-08-31, ended `2026-08-31T20:55Z` (UTC)
**Environment:** GitHub Pages staging preview
`https://drlizlondon.github.io/mybishbash-preview/`
**Deployed build:** `sourceSha 2e001480402adfa87d2e11b7b03e73fd25d3946f`
(`version.json`, no-store) — contains required correction `8c4824d`. ✅
**Browser/engine:** Chromium (Chrome 148), disposable profile, mobile viewport
emulation (Pixel 8 UA). Signed out throughout. Synthetic data only.

## Operator boundary — READ THIS

Per `manual-verification-packet-2026-08-02.md`, an agent **may prepare and drive
the fixture but must not claim the gate passed**. This run was **driven by Claude
(the agent) at Lizzie's explicit request**, with each checkpoint's result
captured below. **The gate is therefore NOT marked passed by the agent.**

> **Founder attestation — SIGNED.** _"Witnessed and attested, Lizzie Soyode,
> 2026-08-31."_ Lizzie reviewed the captured checkpoint results reported by the
> agent-driven run and accepts them as what the application did. **Packet A
> (staging kill-switch) is CLOSED.**

Note on method: the build has a **cosmetic z-index bug** — the card's ••• menu
paints behind the card above it, so the on-screen **Edit** button was not
mouse-clickable in this preview. Card edits were therefore made by invoking the
card's genuine **Edit** handler and **Save myBishBash** button programmatically
(`element.click()`) and setting the text field via the native React value setter
(fires the real `onChange`). This drives the app's **real edit/save write path** —
it does not write storage directly. The z-index bug is a separate cosmetic
finding (already reflected in the repo's account-menu fix branches); it does not
affect persistence behaviour.

## Storage keys inspected at each checkpoint

`localStorage mybishbash.cards.v1`, `…storage-engine.v1`,
`…storage-migration-retry.v1`, `…storage-migration-retry-ack.v1`;
IndexedDB `mybishbash/kv → mybishbash.cards.v1`, `mybishbash/meta →
migratedFromLocalStorage`.

## Results

| CP | Action | Card on screen | IndexedDB card | localStorage card | Migrated marker | Engine | Retry REQUEST | Retry ACK | Matches packet? |
|----|--------|----------------|----------------|-------------------|-----------------|--------|---------------|-----------|-----------------|
| **A0** | Seed cleared profile + first boot | `Legacy seed` | `Legacy seed` | `Legacy seed` | PRESENT | default (IDB) | (none) | (none) | ✅ |
| **A1** | UI edit → `IDB edit`; reload | `IDB edit` | `IDB edit` | `Legacy seed` | PRESENT | default (IDB) | (none) | (none) | ✅ |
| **A2** | Kill switch ON (`localstorage`) + reload | `Legacy seed` | `IDB edit` | `Legacy seed` | PRESENT | localstorage | **(none)** | (none) | ✅ |
| **A3** | UI edit → `Legacy return` (on localStorage) | `Legacy return` | `IDB edit` | `Legacy return` | — | localstorage | **`1636d272-…-f3dc240a87c8`** | (none) | ✅ |
| **A4** | Kill switch OFF + reload | `Legacy return` | `Legacy return` | `Legacy return` | PRESENT | default (IDB) | `1636d272-…` | **`1636d272-…` (= REQUEST)** | ✅ |
| **A5** | Reload again | `Legacy return` | `Legacy return` | — | PRESENT | default (IDB) | — | — | ✅ |

Full A3/A4 retry token: `1636d272-2221-481e-a0e2-f3dc240a87c8` (request at A3 =
acknowledgement at A4).

## The two safety properties the packet cares about — both held

1. **A2 — flipping the kill switch did NOT publish replay authority.** The retry
   REQUEST stayed `(none)` after the switch-and-reload; the A1 IndexedDB edit
   (`IDB edit`) was **not** overwritten by the stale localStorage snapshot. This
   is precisely the pre-edit replay path that blocked candidate `ec4f715`; the
   `8c4824d` correction holds on the deployed build.
2. **A3→A4 — only the genuine legacy edit advanced authority, and it reconciled
   exactly.** The A3 UI edit created one retry REQUEST; removing the switch at A4
   reconciled that edit into IndexedDB and set ACK equal to that same token. The
   value survived the A5 reload. Migration marker stayed PRESENT and single
   throughout.

## Network / console

- **Network:** every request was static assets (`index-*.js`, `react-*.js`,
  `supabase-*.js` bundle, `index-*.css`, logo PNG) + `version.json`. **Zero**
  `get_sync_v2_assignment`, entity, receipt, queue, bootstrap, `/rest/`, or
  `/functions/` transport requests. ✅ (matches the packet's Sync-v2-silence
  requirement.)
- **Console:** only benign `404` entries for `/library` (GitHub Pages static 404
  before the SPA `?route=` fallback loads — the app then renders normally). No
  storage errors, no uncaught exceptions.

## Verdict (subject to founder attestation)

Every checkpoint A0–A5 matched the packet's expected authority table; the switch
never clobbered newer IDB state; the genuine legacy edit reconciled with a
matching retry token; no Sync v2 traffic occurred. Packet A's pass conditions are
met and **founder-attested 2026-08-31 (signature above) → Packet A CLOSED.**

**Packet B (installed iPhone) remains outstanding.** Phase 5 stays *Blocked*
until Packet B is also attested — one of its two manual gates is now cleared.
